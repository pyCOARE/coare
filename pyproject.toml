[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "Python"
version = "0.1"
requires-python = ">=3.7"
license = {text = "MIT License"}
dependencies = [
    "meteo",
    "numpy"
]
